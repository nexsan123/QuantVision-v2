# QuantVision v2.1 用户流程与页面交互修复计划

> **计划周期**: 1-2周
> **目标**: 完善用户使用流程逻辑、修复页面交互断点
> **视角**: 金融机构 / 量化交易者 / 全栈工程师

---

## 目录

1. [问题诊断](#1-问题诊断)
2. [专业量化交易者的使用流程](#2-专业量化交易者的使用流程)
3. [页面职责重新定义](#3-页面职责重新定义)
4. [核心交互修复方案](#4-核心交互修复方案)
5. [详细修复任务](#5-详细修复任务)
6. [数据模型调整](#6-数据模型调整)
7. [执行计划](#7-执行计划)

---

## 1. 问题诊断

### 1.1 当前页面结构分析

| 页面 | 当前职责 | 问题 |
|------|----------|------|
| **我的策略** | 显示Deployment(部署实例) | 概念混淆：策略≠部署 |
| **策略模板** | 选模板→直接部署 | 跳过了策略保存和回测 |
| **策略构建** | 7步向导创建策略 | 保存仅console.log，无实际持久化 |
| **回测中心** | 配置+运行回测 | 无策略选择器，与策略库断开 |
| **交易执行** | 监控交易信号 | 正常 |

### 1.2 关键流程断点

```
当前断点示意图:

策略模板 ─────────────────────────────→ 直接部署 (跳过回测!)
                                         ↑ 问题1

策略构建 ──→ 保存(假) ──→ ? (无下一步)
                          ↑ 问题2

回测中心 ──→ 选策略(无) ──→ 运行 ──→ 完成(无引导)
              ↑ 问题3                  ↑ 问题4

我的策略 ──→ 显示Deployment ──→ 无法查看策略配置
              ↑ 问题5
```

### 1.3 概念混淆问题

**当前代码的混淆**:
```typescript
// MyStrategies/index.tsx - 实际显示的是Deployment
const mockDeployments: Deployment[] = [...]  // 命名为Deployment

// 但页面标题是"我的策略"
<h1>我的策略</h1>
```

**正确的概念区分**:

| 概念 | 英文 | 说明 | 生命周期 |
|------|------|------|----------|
| 策略配置 | Strategy | 策略的定义和参数 | 草稿→回测→就绪 |
| 策略部署 | Deployment | 策略的运行实例 | 模拟盘→实盘→暂停 |

---

## 2. 专业量化交易者的使用流程

### 2.1 完整用户旅程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        专业量化交易者使用流程                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Phase 1: 研究阶段                                                          │
│  ┌─────────────┐                                                            │
│  │ 📊 因子实验室 │ → 研究因子有效性 → 确定Alpha来源                           │
│  └─────────────┘                                                            │
│         │                                                                   │
│         ▼                                                                   │
│  Phase 2: 策略创建                                                          │
│  ┌─────────────┐    ┌─────────────┐                                        │
│  │ 📋 策略模板  │ OR │ 🔧 策略构建  │ → 创建策略配置                          │
│  └─────────────┘    └─────────────┘                                        │
│         │                  │                                                │
│         └────────┬─────────┘                                                │
│                  ▼                                                          │
│  ┌───────────────────────────────┐                                          │
│  │ 💾 保存到「我的策略」(草稿)    │                                          │
│  └───────────────────────────────┘                                          │
│                  │                                                          │
│                  ▼                                                          │
│  Phase 3: 回测验证                                                          │
│  ┌─────────────┐                                                            │
│  │ 📈 回测中心  │ → 选择策略 → 运行回测 → 分析结果                           │
│  └─────────────┘                                                            │
│         │                                                                   │
│         ├─── 不满意 → 返回修改策略                                           │
│         │                                                                   │
│         └─── 满意 → 标记"回测通过"                                           │
│                  │                                                          │
│                  ▼                                                          │
│  Phase 4: 部署运行                                                          │
│  ┌─────────────┐    ┌─────────────┐                                        │
│  │ 🔵 模拟盘   │ →  │ 🟢 实盘     │ (需30天模拟盘验证)                       │
│  └─────────────┘    └─────────────┘                                        │
│                           │                                                 │
│                           ▼                                                 │
│  Phase 5: 监控与复盘                                                        │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                     │
│  │ 📡 交易执行  │    │ ⚠️ 风险中心  │    │ 🔄 策略回放  │                     │
│  │ 信号监控    │    │ 风险预警    │    │ 历史复盘    │                     │
│  └─────────────┘    └─────────────┘    └─────────────┘                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 页面跳转流程图

```
                                    ┌──────────────────┐
                                    │     Dashboard    │
                                    │     (入口)       │
                                    └────────┬─────────┘
                                             │
            ┌────────────────────────────────┼────────────────────────────────┐
            │                                │                                │
            ▼                                ▼                                ▼
    ┌───────────────┐              ┌───────────────┐              ┌───────────────┐
    │   因子实验室   │              │  策略模板库   │              │   我的策略    │
    │  (研究因子)   │              │ (快速开始)    │              │ (管理策略)    │
    └───────┬───────┘              └───────┬───────┘              └───────┬───────┘
            │                              │                              │
            │ 找到有效因子                  │ 选择模板                      │ 选择策略
            │                              │                              │
            ▼                              ▼                              │
    ┌───────────────┐              ┌───────────────┐                      │
    │   策略构建器   │◄─────────────│  使用模板创建  │                      │
    │  (7步向导)    │   预填参数    │   策略副本    │                      │
    └───────┬───────┘              └───────────────┘                      │
            │                                                             │
            │ 保存策略                                                     │
            │                                                             │
            ▼                                                             │
    ┌───────────────┐                                                     │
    │   我的策略    │◄────────────────────────────────────────────────────┘
    │ (策略列表)    │
    └───────┬───────┘
            │
            │ 选择策略 → 运行回测
            │
            ▼
    ┌───────────────┐
    │   回测中心    │
    │ (验证策略)    │
    └───────┬───────┘
            │
            │ 回测完成
            │
            ▼
    ┌────────────────────────────────┐
    │  回测完成弹窗                   │
    │  [查看详情] [部署策略] [修改]   │
    └───────┬────────────────────────┘
            │
            │ 点击"部署策略"
            │
            ▼
    ┌───────────────┐
    │  部署向导     │
    │ (5步配置)     │
    └───────┬───────┘
            │
            │ 完成部署
            │
            ▼
    ┌───────────────┐        ┌───────────────┐
    │   交易执行    │◄──────►│   风险中心    │
    │ (信号监控)    │        │ (风险管理)    │
    └───────┬───────┘        └───────────────┘
            │
            │ 复盘分析
            │
            ▼
    ┌───────────────┐
    │   策略回放    │
    │ (历史分析)    │
    └───────────────┘
```

---

## 3. 页面职责重新定义

### 3.1 页面职责矩阵

| 页面 | 主要职责 | 入口数据 | 出口数据 | 导航目标 |
|------|----------|----------|----------|----------|
| **仪表盘** | 概览、快捷入口 | - | - | 各页面 |
| **因子实验室** | 因子研究验证 | - | 有效因子 | 策略构建 |
| **策略模板** | 快速创建策略 | - | 策略草稿 | 我的策略 |
| **策略构建** | 自定义策略 | 模板/空白 | 策略草稿 | 我的策略 |
| **我的策略** | 策略管理中心 | - | 选中策略 | 回测/部署 |
| **回测中心** | 策略回测验证 | 策略ID | 回测结果 | 部署向导 |
| **交易执行** | 信号监控交易 | 部署ID | 交易记录 | 风险中心 |
| **风险中心** | 风险监控预警 | 部署ID | 风险报告 | - |
| **策略回放** | 历史复盘分析 | 策略ID | 分析报告 | - |

### 3.2 「我的策略」页面重构

**当前问题**: 显示Deployment，命名为"我的策略"

**修复方案**: 拆分为两个Tab或两个独立区域

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  📋 我的策略                                          [+ 新建策略] [导入]    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [策略库]  [运行中]                                                          │
│  ─────────────────                                                          │
│                                                                             │
│  ┌─ 策略库 Tab ─────────────────────────────────────────────────────────┐   │
│  │                                                                       │   │
│  │  状态筛选: [全部] [草稿] [回测通过] [已部署]                           │   │
│  │                                                                       │   │
│  │  ┌───────────────────────────────────────────────────────────────┐   │   │
│  │  │ 📝 价值投资策略 v1.2                              草稿 ⚪       │   │   │
│  │  │    类型: 长线 | 因子: PE,ROE | 上次修改: 2025-01-05            │   │   │
│  │  │    [编辑] [回测] [删除]                                        │   │   │
│  │  └───────────────────────────────────────────────────────────────┘   │   │
│  │                                                                       │   │
│  │  ┌───────────────────────────────────────────────────────────────┐   │   │
│  │  │ ✅ 动量突破策略                                  回测通过 🟢    │   │   │
│  │  │    类型: 短线 | 回测收益: +25.3% | 夏普: 1.85                   │   │   │
│  │  │    [编辑] [查看回测] [部署] [删除]                              │   │   │
│  │  └───────────────────────────────────────────────────────────────┘   │   │
│  │                                                                       │   │
│  └───────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─ 运行中 Tab ─────────────────────────────────────────────────────────┐   │
│  │                                                                       │   │
│  │  环境筛选: [全部] [🔵模拟盘] [🟢实盘]                                 │   │
│  │                                                                       │   │
│  │  ┌───────────────────────────────────────────────────────────────┐   │   │
│  │  │ 🟢 动量突破策略 (实盘)                           运行中        │   │   │
│  │  │    今日: +$385 (+0.8%) | 持仓: 5只                              │   │   │
│  │  │    [监控] [暂停] [设置]                                        │   │   │
│  │  └───────────────────────────────────────────────────────────────┘   │   │
│  │                                                                       │   │
│  └───────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 导航菜单调整建议

**当前菜单**:
```
仪表盘
我的策略     ← 概念混淆
策略模板
因子实验室
策略构建
策略回放
回测中心
交易执行
日内交易
风险中心
```

**建议菜单** (按使用流程排序):
```
仪表盘
─────────────
📚 策略研究
├── 因子实验室
├── 策略模板
└── 策略构建
─────────────
📋 策略管理
├── 我的策略    ← 策略库 + 运行中
└── 回测中心
─────────────
💹 交易执行
├── 交易监控
├── 日内交易
└── 风险中心
─────────────
📊 分析复盘
└── 策略回放
```

---

## 4. 核心交互修复方案

### 4.1 修复1: 策略模板 → 我的策略

**当前流程**: 模板 → 直接部署
**正确流程**: 模板 → 创建策略副本 → 保存到我的策略 → (可选)进入编辑

```typescript
// Templates/index.tsx - 修改handleDeploy
const handleUseTemplate = async (template: StrategyTemplate) => {
  // 1. 基于模板创建策略草稿
  const newStrategy = {
    name: `${template.name} (副本)`,
    source: 'template',
    templateId: template.template_id,
    config: template.strategy_config,
    status: 'draft',
  };

  // 2. 保存到后端
  const savedStrategy = await api.createStrategy(newStrategy);

  // 3. 提示用户选择下一步
  Modal.confirm({
    title: '策略已创建',
    content: `基于「${template.name}」创建了新策略`,
    okText: '去编辑',
    cancelText: '查看策略列表',
    onOk: () => navigate(`/strategy/edit/${savedStrategy.id}`),
    onCancel: () => navigate('/my-strategies'),
  });
};
```

### 4.2 修复2: 策略构建 → 保存 → 我的策略

**当前流程**: 保存 → console.log (假保存)
**正确流程**: 保存 → API持久化 → 提示下一步

```typescript
// StrategyBuilder/index.tsx - 修改handleSave
const handleSave = async () => {
  try {
    // 1. 调用API保存
    const response = await api.saveStrategy(config);

    // 2. 更新本地状态
    setConfig(prev => ({ ...prev, id: response.id }));
    setIsDirty(false);

    // 3. 提示下一步
    Modal.success({
      title: '策略已保存',
      content: (
        <div>
          <p>策略「{config.name}」已保存到您的策略库</p>
          <p className="text-gray-400 mt-2">下一步您可以：</p>
        </div>
      ),
      okText: '运行回测',
      cancelText: '继续编辑',
      onOk: () => navigate(`/backtest?strategyId=${response.id}`),
    });
  } catch (error) {
    message.error('保存失败');
  }
};
```

### 4.3 修复3: 回测中心 ← 策略选择器

**当前问题**: 回测中心没有策略选择，硬编码Mock数据
**修复方案**: 添加策略选择器，支持从URL参数或下拉选择

```typescript
// BacktestCenter/index.tsx - 添加策略选择
const BacktestCenter = () => {
  const [searchParams] = useSearchParams();
  const preselectedStrategyId = searchParams.get('strategyId');

  const [strategies, setStrategies] = useState<Strategy[]>([]);
  const [selectedStrategyId, setSelectedStrategyId] = useState(preselectedStrategyId);

  useEffect(() => {
    // 获取用户的策略列表
    api.getMyStrategies({ status: ['draft', 'backtest_passed'] })
      .then(setStrategies);
  }, []);

  return (
    <Card title="回测配置">
      <Row gutter={[16, 16]}>
        <Col span={8}>
          <div className="text-sm text-gray-400 mb-2">选择策略</div>
          <Select
            value={selectedStrategyId}
            onChange={setSelectedStrategyId}
            placeholder="请选择要回测的策略"
            style={{ width: '100%' }}
          >
            {strategies.map(s => (
              <Select.Option key={s.id} value={s.id}>
                {s.name} ({s.status === 'draft' ? '草稿' : '已回测'})
              </Select.Option>
            ))}
          </Select>
        </Col>
        {/* ... 其他配置 */}
      </Row>
    </Card>
  );
};
```

### 4.4 修复4: 回测完成 → 下一步弹窗

**当前问题**: 回测完成后无引导
**修复方案**: 添加回测完成弹窗

```typescript
// 新建 BacktestCompleteModal.tsx
interface BacktestCompleteModalProps {
  visible: boolean;
  strategy: Strategy;
  result: BacktestResult;
  onViewDetails: () => void;
  onDeploy: () => void;
  onModify: () => void;
  onClose: () => void;
}

const BacktestCompleteModal = ({
  visible, strategy, result, onViewDetails, onDeploy, onModify, onClose
}) => (
  <Modal
    title="✅ 回测完成"
    open={visible}
    footer={null}
    onCancel={onClose}
  >
    <div className="space-y-4">
      <div className="text-lg font-medium">{strategy.name}</div>

      {/* 核心指标 */}
      <div className="grid grid-cols-4 gap-4 bg-dark-card p-4 rounded">
        <div className="text-center">
          <div className="text-gray-400 text-xs">总收益</div>
          <div className={result.totalReturn >= 0 ? 'text-green-400' : 'text-red-400'}>
            {(result.totalReturn * 100).toFixed(1)}%
          </div>
        </div>
        {/* 夏普、回撤、胜率... */}
      </div>

      {/* 下一步选择 */}
      <div className="text-gray-400 mb-2">下一步您想要：</div>
      <div className="grid grid-cols-3 gap-3">
        <Button icon={<BarChartOutlined />} onClick={onViewDetails}>
          查看详情
        </Button>
        <Button type="primary" icon={<RocketOutlined />} onClick={onDeploy}>
          部署策略
        </Button>
        <Button icon={<EditOutlined />} onClick={onModify}>
          返回修改
        </Button>
      </div>
    </div>
  </Modal>
);
```

### 4.5 修复5: 我的策略页面重构

**修复方案**: 使用Tab区分「策略库」和「运行中」

```typescript
// MyStrategies/index.tsx - 重构
const MyStrategiesPage = () => {
  const [activeTab, setActiveTab] = useState<'library' | 'running'>('library');

  return (
    <div className="space-y-6">
      <div className="flex justify-between items-center">
        <h1 className="text-2xl font-bold">我的策略</h1>
        <Space>
          <Button type="primary" icon={<PlusOutlined />} onClick={() => navigate('/strategy')}>
            新建策略
          </Button>
          <Button onClick={() => navigate('/templates')}>使用模板</Button>
        </Space>
      </div>

      <Tabs
        activeKey={activeTab}
        onChange={setActiveTab}
        items={[
          {
            key: 'library',
            label: (
              <span>
                <FolderOutlined /> 策略库
                <Badge count={strategies.length} className="ml-2" />
              </span>
            ),
            children: <StrategyLibraryTab />,  // 显示策略配置
          },
          {
            key: 'running',
            label: (
              <span>
                <PlayCircleOutlined /> 运行中
                <Badge count={deployments.filter(d => d.status === 'running').length} className="ml-2" />
              </span>
            ),
            children: <RunningStrategiesTab />,  // 显示部署实例
          },
        ]}
      />
    </div>
  );
};
```

---

## 5. 详细修复任务

### 5.1 任务清单

| ID | 任务 | 优先级 | 预估 | 依赖 |
|----|------|:------:|:----:|:----:|
| F1 | 新建Strategy数据模型和API | P0 | 4h | - |
| F2 | 修改策略构建器保存逻辑 | P0 | 3h | F1 |
| F3 | 修改策略模板使用逻辑 | P0 | 2h | F1 |
| F4 | 重构我的策略页面(Tab) | P0 | 6h | F1 |
| F5 | 新建StrategyLibraryTab组件 | P0 | 4h | F4 |
| F6 | 新建RunningStrategiesTab组件 | P0 | 3h | F4 |
| F7 | 回测中心添加策略选择器 | P0 | 3h | F1 |
| F8 | 新建回测完成弹窗组件 | P0 | 4h | - |
| F9 | 回测中心集成完成弹窗 | P0 | 2h | F8 |
| F10 | 部署向导添加股票池配置 | P1 | 4h | - |
| F11 | 部署向导添加执行模式 | P1 | 2h | - |
| F12 | 部署向导添加前置检查 | P1 | 3h | - |
| F13 | 后端API补充 | P0 | 6h | F1 |
| F14 | 集成测试 | P0 | 4h | All |

**总计**: ~50h (约1.5周)

### 5.2 任务依赖图

```
F1 (Strategy模型)
├── F2 (策略构建保存)
├── F3 (模板使用逻辑)
├── F4 (我的策略重构)
│   ├── F5 (策略库Tab)
│   └── F6 (运行中Tab)
├── F7 (回测策略选择)
└── F13 (后端API)

F8 (回测完成弹窗)
└── F9 (回测集成弹窗)

F10, F11, F12 (部署向导增强) - 可并行

F14 (集成测试) - 最后执行
```

---

## 6. 数据模型调整

### 6.1 新增Strategy模型

```typescript
// types/strategy.ts - 新增

// 策略状态 (配置层面)
export type StrategyStatus =
  | 'draft'           // 草稿
  | 'backtest_passed' // 回测通过
  | 'deployed';       // 已部署

// 策略来源
export type StrategySource =
  | 'custom'    // 自定义创建
  | 'template'  // 基于模板
  | 'import';   // 导入

// 策略实体 (配置)
export interface Strategy {
  id: string;
  userId: string;
  name: string;
  description?: string;
  source: StrategySource;
  templateId?: string;  // 如果来自模板

  // 策略配置 (7步向导的结果)
  config: StrategyConfig;

  // 状态
  status: StrategyStatus;

  // 回测信息 (如果已回测)
  lastBacktest?: {
    backtestId: string;
    totalReturn: number;
    sharpe: number;
    maxDrawdown: number;
    winRate: number;
    runAt: string;
  };

  // 部署信息 (如果已部署)
  deployments?: {
    deploymentId: string;
    environment: 'paper' | 'live';
    status: 'running' | 'paused';
  }[];

  // 版本控制
  version: number;

  // 时间戳
  createdAt: string;
  updatedAt: string;
}
```

### 6.2 API端点设计

```typescript
// 策略管理API
GET    /api/v1/strategies                    // 获取策略列表
GET    /api/v1/strategies/:id                // 获取策略详情
POST   /api/v1/strategies                    // 创建策略
PUT    /api/v1/strategies/:id                // 更新策略
DELETE /api/v1/strategies/:id                // 删除策略
POST   /api/v1/strategies/:id/clone          // 克隆策略
POST   /api/v1/strategies/from-template      // 从模板创建

// 回测关联
POST   /api/v1/strategies/:id/backtest       // 运行回测
GET    /api/v1/strategies/:id/backtests      // 获取回测历史

// 部署关联
POST   /api/v1/strategies/:id/deploy         // 部署策略
GET    /api/v1/strategies/:id/deployments    // 获取部署列表
```

### 6.3 后端Schema

```python
# backend/app/schemas/strategy.py

class StrategyStatus(str, Enum):
    DRAFT = "draft"
    BACKTEST_PASSED = "backtest_passed"
    DEPLOYED = "deployed"

class StrategySource(str, Enum):
    CUSTOM = "custom"
    TEMPLATE = "template"
    IMPORT = "import"

class StrategyBase(BaseModel):
    name: str
    description: Optional[str]
    source: StrategySource
    template_id: Optional[str]
    config: Dict[str, Any]  # StrategyConfig JSON

class StrategyCreate(StrategyBase):
    pass

class StrategyUpdate(BaseModel):
    name: Optional[str]
    description: Optional[str]
    config: Optional[Dict[str, Any]]

class StrategyResponse(StrategyBase):
    id: str
    user_id: str
    status: StrategyStatus
    version: int
    last_backtest: Optional[Dict[str, Any]]
    deployments: Optional[List[Dict[str, Any]]]
    created_at: datetime
    updated_at: datetime
```

---

## 7. 执行计划

### 7.1 Sprint安排

```
Sprint 1: 基础模型 (Day 1-2)
├── F1: Strategy数据模型
├── F13: 后端API (基础部分)
└── 验收: 可通过API创建/读取策略

Sprint 2: 核心流程修复 (Day 3-5)
├── F2: 策略构建保存逻辑
├── F3: 模板使用逻辑
├── F7: 回测策略选择器
├── F8: 回测完成弹窗
└── F9: 回测集成弹窗
└── 验收: 策略创建→保存→回测流程打通

Sprint 3: 我的策略重构 (Day 6-7)
├── F4: 我的策略页面重构
├── F5: 策略库Tab
└── F6: 运行中Tab
└── 验收: 我的策略页面可正常使用

Sprint 4: 部署增强 (Day 8-9)
├── F10: 股票池配置
├── F11: 执行模式
└── F12: 前置检查
└── 验收: 完整部署流程

Sprint 5: 测试与收尾 (Day 10)
├── F14: 集成测试
└── Bug修复
└── 验收: 全流程可用
```

### 7.2 验收标准

| 场景 | 验收条件 |
|------|----------|
| 使用模板创建策略 | 模板→创建副本→保存→跳转我的策略 |
| 自定义创建策略 | 7步向导→保存→提示下一步 |
| 查看我的策略 | 策略库Tab显示配置，运行中Tab显示部署 |
| 运行回测 | 选择策略→运行→完成弹窗→可部署 |
| 部署策略 | 5步向导→含股票池/执行模式→模拟盘 |
| 切换实盘 | 前置检查通过→风险签署→实盘运行 |

### 7.3 风险点

| 风险 | 可能性 | 影响 | 应对 |
|------|:------:|:----:|------|
| Strategy与Deployment概念混淆 | 中 | 高 | 明确文档定义 |
| 数据迁移问题 | 低 | 中 | 保持Deployment向后兼容 |
| 页面跳转逻辑复杂 | 中 | 中 | 画流程图确认 |

---

## 附录

### A. 现有页面文件位置

```
frontend/src/pages/
├── Dashboard/index.tsx
├── MyStrategies/index.tsx      ← 需重构
├── Templates/index.tsx         ← 需修改
├── FactorLab/index.tsx
├── StrategyBuilder/index.tsx   ← 需修改
├── StrategyReplay/index.tsx
├── BacktestCenter/index.tsx    ← 需修改
├── Trading/index.tsx
├── TradingStream/index.tsx
└── RiskCenter/index.tsx
```

### B. 需新建的组件

```
frontend/src/components/
├── Backtest/
│   ├── BacktestCompleteModal.tsx   ← 新建
│   ├── StrategySelector.tsx        ← 新建
│   └── index.ts
├── MyStrategies/
│   ├── StrategyLibraryTab.tsx      ← 新建
│   ├── RunningStrategiesTab.tsx    ← 新建
│   ├── StrategyCard.tsx            ← 新建
│   └── index.ts
```

---

*计划创建时间: 2025-01-06*
*预计完成时间: 2025-01-17*
